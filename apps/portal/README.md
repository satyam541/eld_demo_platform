# Portal — Next.js 15 fleet manager

> Placeholder. Scaffold per
> [Phase D1/D2](../../../execution-plan/04-phase-d-web-portal.md):
>
> ```powershell
> cd apps
> pnpm dlx create-next-app@latest portal --ts --tailwind --eslint --app --src-dir --import-alias "@/*"
> ```
>
> Then re-add the `@eld/db`, `@eld/shared`, `@eld/ui` workspace dependencies
> from the placeholder `package.json` and the dependency list in Phase D2.
